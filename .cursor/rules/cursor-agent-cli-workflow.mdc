---
description: Cursor Agents only — vs Claude Code CLI; routing, handoffs, subagents.
alwaysApply: true
---

> **Repo rule.** Same text in **User rules:** paste from `~/.cursor/USER-RULES-PASTE-FOR-CURSOR-AGENTS.md` (**Cursor → Settings → Rules**). **Canonical source:** edit **`~/Desktop/Cursor Rules/repo-init.sh`** (`write_cursor_agent_cli_workflow_mdc`), then re-run **phase 2** or **`sync-copies`**. New repo: copy `repo-init.sh` into the repo root, run **`./repo-init.sh`** (full or at least phases **2+** so this file is written).

# Cursor Agents view — workflow

**Scope:** These rules apply **only** in **Cursor Agent** (Agents side view / agent chat, e.g. Cmd+I). They do **not** apply to Cursor Tab, Cmd/Ctrl+K inline edit, or the terminal by themselves.

**Outside this view:** Long or wide work may belong in **Claude Code** (`claude` in the terminal — e.g. Claude Max subscription, separate from Cursor billing). **`~/.claude/CLAUDE.md`** is the global companion for the CLI; keep **`CLAUDE.md`** in this repo aligned when workflow wording changes.

## What each surface is best at

- **Cursor Agents (and subagents when helpful):** IDE-tight work (navigation, diffs, diagnostics), MCP or browser in the loop, many short steering cycles, UI/layout iteration.
- **Claude Code** (`claude` at repo root): Long linear passes, wide refactors, long chains of shell/git/test commands, sustained terminal-driven work.
- **Rule of thumb:** IDE-shaped → Cursor. Batch-shaped → Claude Code.

## Roles (while in Agents view)

- You are the agent in **Cursor Agents view**; coordinate with **Claude Code** when that is the better surface.
- **Before acting on non-trivial work**, do this explicitly: (1) recommend **Cursor Agents** vs **`claude` at repo root** plus **one sentence why**; (2) confirm **one driver** for this branch — only this chat **or** only `claude` until handoff; (3) list **constraints**: branch, what must not change, how to verify (test/command); (4) if the other surface is better, output a filled **HANDOFF** block (template below) for the user to paste there.
- If the task **changes shape** (e.g. short fix → large refactor), say so and suggest switching.
- Prefer **Claude Code** for complex or long-running **batch** work; **suggest a handover** when that fits better than continuing in this chat.
- Human is the final decision maker on branches, commits, and pushes.
- Ask before creating branches, committing, or pushing.
- Keep changes small and reviewable.

## Subagents (Cursor)

Use **subagents** for scoped side quests (e.g. read-only exploration, parallel lookups) that would clutter the main thread. Do **not** split **write ownership** between subagents and the main agent on the same branch without an explicit **handoff** summary.

## Session bias (this chat)

Use subagents to reduce thrash. If the task becomes a **long terminal-driven pass**, stop extending here — output a **HANDOFF** block for **Claude Code** instead of grinding in Agents.

## Cursor Agent vs Claude Code (same machine)

- Only **one autonomous driver per branch** at a time: either Cursor Agent **or** `claude`, not both editing the same pass. Before switching, leave a clean story (`git status` understandable, or a commit).
- The user runs `claude` from the repo root (`git rev-parse --show-toplevel`). Prefer one Cursor window = one repo root aligned with that cwd.

**HANDOFF block** — when switching surfaces, output this **filled in** for the user to paste into the other tool:

```
HANDOFF — read first, then continue.

Goal:
Non-goals / do not touch:
Branch:  (who owns this pass: Cursor vs claude)
Already done:  (files + summary)
Commands already run:  (with outcomes if relevant)
Current state:  (tests/lint, open errors)
Next steps:  (3–5 ordered)
Open questions / risks:
```

## Chat and session hygiene

- **One chat per task.** When a task is fully done and you are moving to something different, say: "Start a new chat for the next task — cleaner context."
- **Context degrading.** If this chat has gone 8+ exchanges without resolving the task, say: "This chat is getting long. Start a new one and re-state the goal."
- **Wrong surface.** If the task has become terminal-driven or spans 5+ files, output a HANDOFF block for Claude Code rather than grinding here.
- **Model.** Always use Auto. Never suggest switching to a specific model.

## Verification and scope

- Before claiming done, run this repo’s usual checks (README, Makefile, `package.json` / `pyproject.toml`, or CI workflow). If none exist, say what you verified manually.
- Do not expand scope: no unrelated refactors, no new dependencies, no extra files unless the user asked or it is required for the task.
- No drive-by formatting or renames outside the requested change.

## Security

- Never commit secrets or paste production credentials into chat. Follow existing env / secret patterns in the repo.

## Subscription vs API (Claude Code)

- When the user intends **Claude Max** subscription billing for the CLI, do not assume `ANTHROPIC_API_KEY` in the shell; subscription auth is separate. If a key is set and they want subscription, call it out.

<!-- setup-ai-context-cursor-cli-workflow v2 -->
