# Copy to a new file (e.g. local.env) and fill in locally. Those files stay gitignored.
# Never commit real values.

# Optional: document-only placeholders — the app does NOT load this file unless you wire it yourself.
# GROK_SESSION_NOTE=use browser login + .playwright/grok_storage.json
